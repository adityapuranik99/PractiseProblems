package Greedy;

public class GeneralStructure {
    getOptimal(int arr[] , int n)
    {
        int res = 0;
        while(all items are not considered)
        {
            i = select an item;
            if(feasible(i))
            {
                res = res + i;
            }
        }
        return res; 
    }
}